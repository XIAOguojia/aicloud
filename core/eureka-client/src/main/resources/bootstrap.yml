spring:
  application:
    name: client.hello
#    config:
#      label: dev
#      profile: dev
#      discovery:
#        enabled: true
#        service-id: config.server


---
spring:
  application:
    name: client.hello
  profiles: docker

server:
  port: ${AICLOUD_CORE_CLIENT_SERVICE_PORT}

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://${AICLOUD_CORE_DISCOVER_SERVICE_HOST}:${}
